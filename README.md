**Requisitos:**
- Tener instalado localmente framework **django**

Sobre `groupExpenses/` ejecutar:
```
python3 manage.py runserver
```
Una vez levantado el servidor web conectarse al `localhost` puerto `8000`. El sitio web es el prototipo v1
